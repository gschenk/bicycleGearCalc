# component: property: type
# types are: boolean, number, text, angle, length, mass, force
bottom bracket:
    width: length
chain:
    elongation: number
    pitch: length
    roller diametre: length
chainring:
    teeth: number array
    chainline: length
chainstay:
    length: length
    offset: length
cog:
    teeth: number array
    chainline: length
drivetrain:
    length: length
dropouts:
    distance: length
    thickness: length
tyre:
    bead diametre: length
    width: length
